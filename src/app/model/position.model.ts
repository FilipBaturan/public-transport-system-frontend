export interface StationPosition {
    id: number;
    latitude: number;
    longitude: number;
    active: boolean;
}

export interface TransportLinePosition {
    id: number;
    content: string;
    active: boolean;
}
